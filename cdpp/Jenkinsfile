pipeline {
    agent any
    stages {
        stage('Checkout SCM') {
            steps {
                git url: 'https://github.com/DongHoonzzi/CDPP.git', credentialsId: 'github_token'
            }
        }
        // 나머지 단계들...
    }
}
